# Dependencies
node_modules
__pycache__
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info

# Build outputs (keep dist/ for frontend builds)
build

# Git
.git
.gitignore

# Environment files
.env
.env.local
.env.*.local

# Logs and databases (we mount these as volumes)
*.log
*.db
*.db-journal
data
logs

# IDE/Editor
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Testing
coverage
.pytest_cache

# Documentation (not needed in container)
docs
*.md
!requirements.txt

# License
LICENSE

# Python project files
pyproject.toml
uv.lock

# Docker meta files (not needed inside container)
docker-compose*.yml
Dockerfile*
.dockerignore
cloudbuild.yaml

# Keep these files for the build (Docker entrypoints are needed)
# - docker-entrypoint.sh (needed for backend)
# - docker-entrypoint-frontend.sh (needed for frontend)
